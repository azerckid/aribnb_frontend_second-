# Frontend environment variables
# Do not commit real secrets. This file documents required variables.

# Django API base URL (no trailing slash recommended for consistency)
# Local dev example:
VITE_API_BASE_URL=http://localhost:8000/api
